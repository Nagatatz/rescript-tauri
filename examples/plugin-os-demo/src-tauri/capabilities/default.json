{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the plugin-os demo",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "os:default"
  ]
}
