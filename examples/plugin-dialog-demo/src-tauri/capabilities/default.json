{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the plugin-dialog demo",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
